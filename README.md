# tree-eclass

## Dependencies

- **maven**

## Usage

```sh
$ ./build.sh && cd prod
$ echo "YOUR_USERNAME" > credentials.txt && echo "YOUR_PASSWORD" >> credentials.txt
$ java -jar tree-eclass.jar
```
